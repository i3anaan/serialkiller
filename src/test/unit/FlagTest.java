package test.unit;

import static org.junit.Assert.*;
import link.angelmaker.nodes.BasicFlag;
import link.angelmaker.nodes.FlaggingNode;

import org.junit.Test;

import util.BitSet2;

public class FlagTest {

	@Test
	public void testBasicFlag() {
		//assertTrue(((BasicFlag)FlaggingNode.FLAG_START_OF_FRAME).alwaysWorks());
		//assertTrue(((BasicFlag)FlaggingNode.FLAG_END_OF_FRAME).alwaysWorks());
	}

}
